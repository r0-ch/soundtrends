{
    "buildCommand": "cd react-client && npm install && npm run build",
    "outputDirectory": "react-client/dist",
    "rewrites": [
        {
            "source": "api/(.*)",
            "destination": "/server/index.js"
        }
    ]
}